/*
    ===== Código de TypeScript =====
*/
let nombre: string = 'Gerson '; //se puede restringir el tipo de dato que se espera optener
let xp: number | string = 'full '; // se puuede inicicar que acepte un dato u otro mediante pipes(|) 
xp =50;
let estaVivo = true;

let avatar = nombre + xp + estaVivo

console.log(avatar);

